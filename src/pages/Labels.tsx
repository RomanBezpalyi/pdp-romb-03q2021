import React, { Fragment } from 'react'

import { LabelsTable } from '../features/labels/components'

export const Labels: React.FC = () => {
  return (
    <Fragment>
      <LabelsTable />
    </Fragment>
  )
}
